/**
 * Containers Index
 *
 * Export container components that connect React to app state
 */

export { ServiceGridContainer } from './ServiceGridContainer.js';
export { TeamGridContainer } from './TeamGridContainer.js';
