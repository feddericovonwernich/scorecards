{
  "name": "scorecards",
  "version": "1.0.0",
  "description": "Automated quality scoring for service repositories",
  "main": "catalog/src/main.js",
  "scripts": {
    "test": "jest",
    "lint": "eslint catalog/src checks/**/*.js",
    "format": "prettier --write '**/*.{js,json,md,yml,yaml}'"
  },
  "keywords": ["quality", "scoring", "github-action", "catalog"],
  "license": "Apache-2.0",
  "dependencies": {
    "js-yaml": "^4.1.0",
    "marked": "^11.1.1",
    "swagger-parser": "^10.0.3"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "eslint": "^8.56.0",
    "prettier": "^3.1.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
